export default function ({ btnText }) {
    return (
        <>
            <button type="button" className="btn btn-primary m-3">
                {btnText}
            </button>
        </>
    );
}
